norme!

cd /home/$USER/Documents/


echo -nnnnnn-n a

env | a a a 

bash expands the var in paths
cat <-- test writing something

< 

export sami="hola mundo"
export COMMAND=/usr/bin/ls

expr $? + $?



echo "$USE'" | echo $"HOM"E

somewhere cmd is being freed before my free funtion. Can you find it and set it cmd->cmd to NULL afterwards?
