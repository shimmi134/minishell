cat 


^c
minishell> minishell> 

echo hi >> sami/shimi

(shoudnt work)

printf 'hello world' >/dev/null | cat -e 

when outfile has a / it doesnt redirect and just prints when it shouldnt


Whenever joe corrects the outfile thing I need to make it so it doesn't exit :3
